doctype html
html
  head
    script(src="https://code.jquery.com/jquery-3.6.0.min.js")

    link(rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/179/179249.png")
    title SYM
    script(src="https://d3js.org/d3.v7.min.js")
    link(rel='stylesheet' type="text/css" href='#{baseUrl}/stylesheets/style.css')
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css")
    script.
      var currentLang = !{JSON.stringify(lang)};
      var baseUrl = !{JSON.stringify(baseUrl)};
  body
    script.
      (function() {
        const currentTheme = localStorage.getItem('theme') || 'light';

        if (currentTheme === 'dark') {
          $('body').addClass('dark');
          $('#theme-toggle').find('i').toggleClass("fa-regular");
          $('#theme-toggle').find('i').toggleClass("fa-solid");
        }
        })();
    block content
